# Platzi Angular Todo App

## Install Angular Cli
```JS
npm i @angular/cli -g
```

## Create new project
```JS
ng new todoapp --skip-tests
```

## Create new component
```JS
ng g component ${dir/component-name}
```